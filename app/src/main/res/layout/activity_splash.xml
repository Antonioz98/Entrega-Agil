<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/acitivity_splash_icone_app"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/margin_standard"
        android:src="@drawable/ic_app"
        app:layout_constraintBottom_toTopOf="@+id/acitivity_splash_titulo_app"
        app:layout_constraintEnd_toEndOf="@+id/acitivity_splash_titulo_app"
        app:layout_constraintStart_toStartOf="@+id/acitivity_splash_titulo_app"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/acitivity_splash_titulo_app"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/margin_layout"
        android:fontFamily="@font/nunito_sans_bold"
        android:text="@string/app_name"
        android:textSize="24sp"
        android:textColor="@color/colorPrimaryText"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/acitivity_splash_subtitulo_app"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/nunito_sans_reguler"
        android:text="@string/subtitulo_app"
        android:textColor="@color/colorPrimaryText"
        android:textSize="@dimen/text_body_size"
        app:layout_constraintStart_toStartOf="@+id/acitivity_splash_titulo_app"
        app:layout_constraintTop_toBottomOf="@+id/acitivity_splash_titulo_app" />

    <ImageView
        android:id="@+id/acitivity_splash_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/margin_ic_splash"
        android:src="@drawable/ic_splash"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:ignore="ContentDescription" />

</androidx.constraintlayout.widget.ConstraintLayout>